<mvc:View controllerName="project1503.controller.Main"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout">
    <Page id="page" title="{i18n>title}">
        <content>
            <l:VerticalLayout >
                <Input id="inputID" placeholder="문자열을 입력하세요."/>
                <l:HorizontalLayout >
                    <FlexBox width="10px"/>
                    <Button text="작성 완료" press="onclick" />
                    <FlexBox width="20px"/>
                    <Button text="초기화" press="onclickRevert" />
                </l:HorizontalLayout>
                <Text id="textID" text=""/>
            </l:VerticalLayout>
        
        </content>
        
    </Page>
</mvc:View>
